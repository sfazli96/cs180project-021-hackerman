const readCSV = require('../modules/parseCSV.js');

function searchCountry(countries){
	
	var concated_data = {};
	// Idea here is to loop through countries selected
	// then concatenate files into one large data structure
	// Something like EXAMPLE: USdata['title'].concat(GRdata['title']);
	for(var i = 0; i <= countries.length; i++){

	}
}

function searchCategory(categories){

}